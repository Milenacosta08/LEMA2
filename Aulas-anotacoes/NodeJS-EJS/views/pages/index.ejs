<!DOCTYPE html>
<html lang="en">
    <!--
        INCLUINDO OUTRAS PÁGINAS
        <%-    %>   ->   comandos EJS
        include     ->   comando de inclusão
    -->
    <%- include('../partials/head'); %>

    <body class="container">
        <%- include('../partials/header'); %>

        <main>
            <div class="jumbotron">
                <h1>Node com EJS</h1>
                <p>Vamos apredner a trabalhar com arquivos EJS</p>
                <p><%= subtitle %></p>
            </div>

            <ul>
                <% qualitys.forEach(function(quality) { %>
                    <li><strong><%= quality.title %></strong> <%= quality.message %></li> 
                <% }) %>
                
            </ul>

        </main>

        <%- include('../partials/footer'); %>
    </body>
</html>